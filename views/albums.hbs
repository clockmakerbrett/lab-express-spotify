<div class="artists-albums">
  {{#each albums}}
      <div class="card">
        <img src="{{ images.[0].url }}" alt="{{ name }}">
        <h4>{{ name }}</h4>
        <a href="/tracks/{{ id }}">View Tracks</a>
      </div>
    {{/each}}
</div>